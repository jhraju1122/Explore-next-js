import React from 'react';

const page = ({params}) => {
    console.log(params.slug);
    return (
        <div className='h-screen '>
            details
        </div>
    );
};

export default page;